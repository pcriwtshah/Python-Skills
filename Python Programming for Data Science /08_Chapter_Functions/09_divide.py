#This program will define a function to divide the two numbers.

#Defining the function
def divide(a,b):
    return float(a/b)
x = int(input("Enter the first number\n"))
y = int(input("Enter the second number\n"))
print("The division of",x, "and", y, "is", divide(x,y))

#Format the output